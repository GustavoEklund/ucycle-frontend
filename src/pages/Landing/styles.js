import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    logo: {
        marginBottom: '20vh',
        width: '100%',
    },
    button: {
        marginBottom: 25,
    },
    link: {
        textAlign: 'center',
        width: '100%',
        display: 'block',
        fontSize: '12pt',
        marginTop: 50,
    },
}));

export default useStyles;
