import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    container: {
        marginTop: '10vh',
    },
    input: {
        marginBottom: 25,
    },
    link: {
        textAlign: 'center',
        width: '100%',
        display: 'block',
        fontSize: '12pt',
        marginTop: 50,
    },
    button: {
        marginTop: 40,
    },
}));

export default useStyles;
