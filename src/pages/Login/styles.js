import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    container: {
        marginTop: '25vh',
    },
    logo: {
        marginBottom: '10vh',
        width: '100%',
    },
    input: {
        marginBottom: 25,
    },
    link: {
        textAlign: 'center',
        width: '100%',
        display: 'block',
        fontSize: '12pt',
        marginTop: 50,
    },
}));

export default useStyles;
